apiVersion: v1
kind: Service
metadata:
  name: {{ include "llm-ollama-openwebui-chart.fullname" . }}
  labels:
    {{- include "llm-ollama-openwebui-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "llm-ollama-openwebui-chart.selectorLabels" . | nindent 4 }}

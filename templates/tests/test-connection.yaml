apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "llm-ollama-openwebui-chart.fullname" . }}-test-connection"
  labels:
    {{- include "llm-ollama-openwebui-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "llm-ollama-openwebui-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
